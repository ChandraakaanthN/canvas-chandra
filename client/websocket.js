// websocket.js (placeholder)
// Socket handling is currently embedded into `main.js` to keep parity with
// the original working code. This file is reserved for moving socket logic
// out later (connect, presence, draw/undo handlers).

export function initWebsocketPlaceholder(){
  // TODO: move socket init and event handlers here
}
